# Raw data - never commit (hundreds of MB). Kept on disk, just not tracked.
DATA Science Files/
data/

# Regenerable intermediates - rebuild by running "R File/Cleaning.R"
cleaned/
outputs/*.sqlite

# The written report is submitted separately, not published here.
# Kept on disk, just not tracked.
*.docx
README.md

# R session files
.Rhistory
.RData
.Rproj.user/
Rplots.pdf
